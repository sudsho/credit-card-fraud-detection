# Core (offline smoke, training, serving). Loose lower bounds so it installs
# on modern Python (3.11+). The old hard pins (scikit-learn 0.22, pandas 1.0,
# Flask 1.1, numpy 1.18) do not build on 3.11, so they were relaxed.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
PyYAML>=6.0
joblib>=1.2
Flask>=2.2
gunicorn>=20.1

# Optional. The smoke and tests do not need these:
#   xgboost      - used by the "xgboost" model_type (make smoke-xgb)
#   imbalanced-learn - real SMOTE; when absent, src.resample falls back to a
#                      numpy random oversampler automatically (see README)
xgboost>=1.7
imbalanced-learn>=0.11

# Dev
pytest>=7.0
