version: "3.7"

services:
  fraud:
    build: .
    image: credit-card-fraud:latest
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      THRESHOLD: 0.5
    volumes:
      - ./models:/app/models:ro
