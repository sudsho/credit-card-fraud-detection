language: python
python:
  - "3.7"
  - "3.8"

install:
  - pip install --upgrade pip
  - pip install -r requirements.txt

script:
  - pytest -q --maxfail=1

cache:
  pip: true

notifications:
  email: false
